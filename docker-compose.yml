
services:
  web:
    build: .
    ports:
      - "127.0.0.1:5000:5000"
    restart: always
    
    env_file:
    - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
